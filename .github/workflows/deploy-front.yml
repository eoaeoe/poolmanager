name: Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - "apps/frontend/**"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy frontend on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            /opt/poolmanager-v2/deploy-front.sh
